SpringServicDaoTemplate :: Demo of Spring REST + Service + Dao and Unit Testing
==========================

1. run project in tomcat + maven
	mvn tomcat:run
2. Test in browser
	http://localhost:8585/demo01/feed/<id>

